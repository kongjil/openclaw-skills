# 2026-03-12

- 用户确认新的默认工作规则：凡是预计超过 5 分钟、需要多步骤推进、工程/运维/排障/持续执行类任务，或用户明确要求“持续做完 / 不要只汇报 / 自动工作”的任务，默认自动进入 `auto-work`。
- 补强强触发短语：`继续，采用自动工作` / `继续采用自动工作` / `继续，用自动工作` / `继续，按自动工作做` / `继续推进，进入自动工作` / `继续，进入自动工作`，必须直接命中 `auto-work`。
- 明确保留风险闸门：deploy / restart / reload / 删除 / 鉴权 / 路由 / 防火墙 / 权限边界改动，即使处于 `auto-work` 中也必须单独确认。
- 一二号机统一建立 `/root/bin/update-auto-work` 与 `/root/bin/auto-work-update` 作为唯一更新入口；旧 installer 已删除。
- `auto-work` 上游统一为 GitHub 仓库：`https://github.com/kongjil/openclaw-skills`。
