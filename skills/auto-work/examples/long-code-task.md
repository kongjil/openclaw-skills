# 长代码任务示例

## 示例 1
`这个 bug 持续做完，不要只监督。`

预期：进入执行型，持续推进 patch → verify → diagnose → patch。

## 示例 2
`这个页面和后端接口联动修改，你直接做完再汇报。`

预期：识别为多文件联动任务，自动进入执行型。

## 示例 3
`这个任务要 build 和 test，别停在中间。`

预期：将 build / test / verify 视为执行链的一部分，而不是阶段终点。
