# 阻塞与确认示例

## deploy 前停止
`代码和本地验证已经完成。下一步需要 deploy 到线上，这会产生真实外部影响，我先停在这里等你确认。`

## restart 前停止
`下一步需要 restart 服务让配置生效，这属于高风险动作，我先不自动执行。`

## delete 前停止
`下一步涉及删除旧文件，这个动作不可逆，我先停下等你确认。`

## 鉴权路由变更前停止
`下一步会改鉴权/路由边界，这属于安全边界变更，我不会默认继续。`

## 缺输入阻塞
`当前卡在缺少页面清单/权限输入，继续推进需要你补这个条件。`
